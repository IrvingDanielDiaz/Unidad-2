(function(){
function getEdad(){
    return 100+300+300;
}
 const nombre     = 'Jonathan';
 const apellido   = 'Lopez'
 const edad       = 33;
 const salida = `${nombre} ${apellido} (${edad})`;

console.log(salida);
})();